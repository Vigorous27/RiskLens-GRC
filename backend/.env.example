DATABASE_URL=postgresql+psycopg://USERNAME:PASSWORD@localhost:5432/DATABASE_NAME
JWT_SECRET=replace_with_secure_random_secret
OPENAI_API_KEY=replace_with_your_openai_api_key  -- a separate .env file should be in the backend folder for the ai because browser side keys can be extracted by anyone.